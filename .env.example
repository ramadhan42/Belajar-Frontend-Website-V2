# Copy to .env.local for local overrides.
# Local Laravel (default for development):
NEXT_PUBLIC_URL=http://127.0.0.1:8000
NEXT_PUBLIC_API_URL=http://127.0.0.1:8000/api

# -----------------------------------------------------------------------------
# Hostinger production frontend (Git branch: main → https://evomi.shop)
# -----------------------------------------------------------------------------
# NEXT_PUBLIC_URL=https://api.evomi.shop
# NEXT_PUBLIC_API_URL=https://api.evomi.shop/api
# NEXT_PUBLIC_SITE_URL=https://evomi.shop

# Vercel uses Git branch `vercel` (do not deploy that branch on Hostinger)
