"use client";

/**
 * Sebelumnya komponen ini logout otomatis di event `pagehide`
 * (termasuk refresh), sehingga user selalu keluar setelah reload.
 * Logout hanya lewat tombol Logout di Navbar / Sidebar Admin.
 */
export default function BeaconListener() {
  return null;
}
